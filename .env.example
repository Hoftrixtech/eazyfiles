MONGODB_URI=
# When Atlas is unreachable locally, allows in-memory usage tracking for image tools (dev only).
ALLOW_LOCAL_MEMORY_FALLBACK=true
NEXT_PUBLIC_SITE_URL=http://localhost:3000

AUTH_SECRET=
AUTH_TRUST_HOST=true

# Google OAuth (Auth.js / next-auth)
# Local callback: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
